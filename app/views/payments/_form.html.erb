
<!-- <%#= simple_form_for(@payment) do |f| %>
  <%#= f.error_notification %>
  <%#= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
  </div>

  <div class="form-actions">
    <%#= f.button :submit %>
  </div>
<%# end %>
 -->



 <%#= form_with model: @payment, local: true do |f| %>
  <%= simple_form_for(@payment) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
 
  <p>
    <%= f.label :card_number%><br>
    <%= f.text_area :card_number %>
  </p>
  <p>
    <%= f.label :expiry_month %><br>
    <%= f.text_area :expiry_month %>
  </p>
  <p>
    <%= f.label :expiry_year %><br>
    <%= f.text_area :expiry_year %>
  </p>
  <p>
    <%= f.label :cvv %><br>
    <%= f.text_area :cvv%>
  </p>
  <p>
    <%= f.label :name_on_the_card %><br>
    <%= f.text_area :name_on_the_card %>
  </p>

 
  <p>
    <%= f.submit :Place_order , class: 'button is-warning'%>
    <%#= link_to 'SUBMIT',order_path, class:'button is-light' %>
  </p>
  <!-- <div class="form-inputs">
  </div>

  <div class="form-actions">
    <%#= f.button :submit %>
  </div> -->
<%end %>